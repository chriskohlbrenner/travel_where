<div>
  <h3>Where are you considering traveling?</h3>
</div>

<%= form_for(@country) do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>
  
  <%= f.submit 'Search Country', class: 'button' %>
<% end %>